你现在是一个顶级的 UI/UX 架构师和 React 专家。我们已经成功通过 `useWeapons` hook 拿到了包含 15 把武器的真实 JSON 数据，现在我们需要把 `App.tsx` 里简陋的 JSON dump 替换成极致酷炫的“赛博朋克风格”商品展示页。

请按照以下要求帮我生成代码：

1. **创建 `WeaponCard` 组件**：
   - 在 `src/components/` 下新建 `WeaponCard.tsx`。
   - 接收一个 `weapon` 对象作为 prop（使用我们定义好的 `Weapon` 类型）。
   - **视觉风格**：极致的赛博朋克与废土风。背景使用深邃的暗灰色或纯黑。边框在鼠标悬浮 (hover) 时，产生霓虹灯般的发光效果（例如电光蓝或荧光绿，请使用 Tailwind 的 ring 或 shadow 配合 hover 伪类）。
   - **卡片内容布局**：
     - 顶部小字或徽章显示 `world_shell`（世界观归属），颜色要低调一点。
     - 醒目的卡片标题 `title`，使用粗体，带有轻微的文字发光或高对比度颜色。
     - 价格区域：将 `price_value` 和 `currency_type` 组合，使用亮色（如赛博黄）突出显示，例如 “2500 废土币”。
     - 描述段落：显示 `short_description`，文字颜色偏灰，保证阅读性。
     - 核心卖点：遍历 `features` 数组，请引入并使用 Shadcn 的 `Badge` 组件来展示这些卖点，Badge 的样式请调整为带有极客感的暗色底+亮色字。

2. **重构 `App.tsx` 布局**：
   - 保留原有的 loading 和 error 状态判断。
   - 移除 `JSON.stringify`。
   - 引入并使用你刚刚创建的 `WeaponCard` 组件。
   - 页面背景设置为深色（如 `bg-zinc-950` 或 `bg-black`），文字默认设为白色。
   - 添加一个酷炫的页面大标题（例如 "DHS 武器库终端"）。
   - 使用 CSS Grid（Tailwind 的 `grid`、`grid-cols-1`、`md:grid-cols-2`、`lg:grid-cols-3`、`gap-6`）将遍历出来的 `WeaponCard` 整齐地排列成响应式网格布局。